import React, { Fragment } from 'react';
import { useAuth } from "../contexts/auth-context";

const Cart = () => {
    return (
        <Fragment>
            <h6>Shopping Cart</h6>
        </Fragment>
    );
};
export default Cart;